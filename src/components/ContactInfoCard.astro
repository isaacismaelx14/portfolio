---
interface Props {
    label: string;
    value: string;
    href: string;
}

const { label, value, href } = Astro.props;
---

<div
    class="flex items-center p-4 bg-white dark:bg-gray-800 rounded-xl border border-gray-100 dark:border-gray-700 shadow-sm"
>
    <div
        class="p-3 bg-gray-100 dark:bg-gray-700 rounded-lg text-gray-600 dark:text-gray-300 mr-4"
    >
        <slot name="icon" />
    </div>
    <div>
        <p class="text-sm font-medium text-gray-500 dark:text-gray-400">
            {label}
        </p>
        <a
            href={href}
            class="text-lg font-semibold text-gray-900 dark:text-white hover:text-blue-600 dark:hover:text-blue-400 transition-colors break-all"
        >
            {value}
        </a>
    </div>
</div>
